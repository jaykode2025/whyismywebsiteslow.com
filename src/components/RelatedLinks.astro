---
type RelatedItem = {
  href: string;
  label: string;
};

const { items = [] } = Astro.props as { items?: RelatedItem[] };
---
<section>
  <h2>Related</h2>
  <ul>
    {items.map((i) => <li><a href={i.href}>{i.label}</a></li>)}
  </ul>
</section>
